<launch>
     <arg name="robot_name" default="hyq"/>

     <!-- Load the robot stuff -->
     <include file="$(eval find(arg('robot_name') + '_description') +'/launch/upload.launch')">
        <arg name="sensors" default="false" />
     </include>

     <!-- Load the controller stuff -->
     <include file="$(find wb_controller)/launch/wb_controller_spawn.launch">
            <arg name="robot_name" value="$(arg robot_name)" />
     </include>

    <group ns="$(arg robot_name)">

          <!-- Controller test -->
          <test test-name="wb_controller_test"
               pkg="wb_controller"
               type="wb_controller_test"
               time-limit="80.0">
         </test>

        <!-- Start the dummy robot -->
         <node name="hyq_dummy"
              pkg="wb_controller"
              output="screen"
              type="hyq_dummy"/>

   </group>

</launch>
